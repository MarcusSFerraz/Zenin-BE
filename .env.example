# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=zenin_db
DB_USER=zenin
DB_PASSWORD=zenin123

# Server
SERVER_PORT=8080
SPRING_PROFILES_ACTIVE=prod

# JWT
# Gere uma chave segura com: openssl rand -base64 32
JWT_SECRET=your_base64_encoded_secret_here
JWT_EXPIRATION_MS=86400000
